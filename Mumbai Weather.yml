info:
  name: Mumbai Weather
  type: http
  seq: 4

http:
  method: GET
  url: https://api.openweathermap.org/data/2.5/weather?q=Mumbai&appid={{API-Key}}
  params:
    - name: q
      value: Mumbai
      type: query
    - name: appid
      value: "{{API-Key}}"
      type: query
  auth:
    type: apikey
    key: "{{API-Key}}"
    placement: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
